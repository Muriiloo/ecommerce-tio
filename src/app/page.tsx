import Header from "@/components/header";
import WhatsappButton from "@/components/whatsappButton";

const Home = () => {
  return (
    <div>
      <Header />
      <WhatsappButton />
    </div>
  );
};

export default Home;
